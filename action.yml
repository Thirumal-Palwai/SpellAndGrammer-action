name: "Spell and Grammar check"
description: "This action helps in cheking spellings and grammer using vale"
branding:
  color: green
  icon: shield

runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.ConfigFile }}
